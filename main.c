#include "main.h"
#include <limits.h>
/**
 * main - Entry point for testing the _printf function.
 *
 * This function tests
 *
 * Return: Always 0 (Success).
 */
int main(void)
{
int p;

p = _printf(NULL);
printf("%i", p);


return (0);
}
