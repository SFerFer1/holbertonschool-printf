#include <stdio.h>
#include <string.h>
#include <unistd.h>
#include "main.h"
int acepta_char(char actual , char anterior)
{

if (anterior == "\" && actual == "n")
{
write(1, "\n", 1);
}
if (anterior == "\" && actual != "n")
{
write(1, &actual, 1);
}




return(1);
}

